export declare type RafFrame = {
    id: number;
};
declare function raf(callback: () => void, delayFrames?: number): {
    id: number;
};
declare namespace raf {
    var cancel: (frame?: {
        id: number;
    }) => void;
}
export default raf;
